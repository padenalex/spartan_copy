# Migration `20200615013804-create_schema`

This migration has been generated by blurdesign at 6/15/2020, 1:38:04 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."manager_user" (
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"manager_id" integer  NOT NULL ,"updated_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"user_id" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."managers" (
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"updated_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"user_id" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."users" (
"address" text   ,"city" text   ,"country" text   ,"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"date_of_birth" timestamp(3)   ,"email" text  NOT NULL ,"id" SERIAL,"name" text  NOT NULL ,"password" text  NOT NULL ,"phone_number" text   ,"post_code" text   ,"state" text   ,"status" text  NOT NULL DEFAULT E'pending',"updated_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "managers.user_id" ON "public"."managers"("user_id")

CREATE UNIQUE INDEX "users.email" ON "public"."users"("email")

ALTER TABLE "public"."manager_user" ADD FOREIGN KEY ("manager_id")REFERENCES "public"."managers"("user_id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."manager_user" ADD FOREIGN KEY ("user_id")REFERENCES "public"."users"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."managers" ADD FOREIGN KEY ("user_id")REFERENCES "public"."users"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200615013804-create_schema
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,46 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+model manager_user {
+  created_at DateTime @default(now())
+  id         Int      @default(autoincrement()) @id
+  manager_id Int
+  updated_at DateTime @default(now())
+  user_id    Int
+  managers   managers @relation(fields: [manager_id], references: [user_id])
+  users      users    @relation(fields: [user_id], references: [id])
+}
+
+model managers {
+  created_at   DateTime       @default(now())
+  id           Int            @default(autoincrement()) @id
+  updated_at   DateTime       @default(now())
+  user_id      Int            @unique
+  users        users          @relation(fields: [user_id], references: [id])
+  manager_user manager_user[]
+}
+
+model users {
+  address       String?
+  city          String?
+  country       String?
+  created_at    DateTime       @default(now())
+  date_of_birth DateTime?
+  email         String         @unique
+  id            Int            @default(autoincrement()) @id
+  name          String
+  password      String
+  phone_number  String?
+  post_code     String?
+  state         String?
+  status        String         @default("pending")
+  updated_at    DateTime       @default(now())
+  manager_user  manager_user[]
+  managers      managers?
+}
```


